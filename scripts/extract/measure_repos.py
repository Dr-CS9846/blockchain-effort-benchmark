#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
measure_repos.py  —  Step 2: clone, measure KSLOC + active person-months
=========================================================================
Reads projects_manifest.csv.  For each row that has a repo_url it:
  1. Clones (or reuses) the repo in repo_cache/<project_id>/
  2. Resolves the commit  (commit_sha > cutoff_date > HEAD)
  3. Counts KSLOC via cloc (preferred) or pygount (fallback)
  4. Counts active person-months from git log (distinct author×month pairs,
     bots and merge commits excluded, .mailmap respected)
  5. Appends a row to measurements.csv

Outputs:
  measurements.csv           – one row per project, status OK/ERROR/NO_REPO
  measurement_provenance.json – tool versions + run parameters

Usage:
  python measure_repos.py                        # all rows
  python measure_repos.py --only AdMeta,Roloi    # subset
  python measure_repos.py --force                # re-measure OK rows too
"""
import argparse, csv, json, os, re, subprocess, sys, datetime, shutil
from pathlib import Path

HERE = Path(__file__).parent.resolve()
CACHE = HERE / "repo_cache"
BOTS = re.compile(r"\[bot\]|github-actions|dependabot|renovate|noreply", re.I)
EXCLUDE_DIRS = ["node_modules", "vendor", ".git", "dist", "build", "target",
                "__pycache__", ".venv", "venv", "third_party", "thirdparty"]

CODE_EXTS = {
    ".rs", ".go", ".ts", ".tsx", ".js", ".jsx", ".py", ".sol", ".c", ".cpp",
    ".cc", ".h", ".hpp", ".java", ".kt", ".swift", ".cs", ".rb", ".scala",
    ".sh", ".bash",
}

# cloc language names that count as SOURCE CODE for the size metric. Excludes
# data/markup/config/docs (JSON, YAML, TOML, Markdown, "PO File", SVG, XML,
# HTML, CSS, Text, INI, Handlebars, Gradle, ...) which otherwise inflate KSLOC.
SOURCE_LANGS = {
    "Rust", "Go", "TypeScript", "JavaScript", "JSX", "TSX", "Python", "Solidity",
    "C", "C++", "C/C++ Header", "C Header", "Java", "Kotlin", "Swift", "C#",
    "Ruby", "Scala", "Vyper", "Move", "Cairo", "Haskell", "Elixir", "Erlang",
    "PHP", "Objective-C", "Dart", "Lua", "Bourne Shell", "Bourne Again Shell",
    # added after census batch-1 review: these are the DELIVERABLE language for some
    # grants and were being undercounted to ~0 (dotnix=Nix, yatima=Lean, ZK=Circom).
    "Nix", "Lean", "Circom",
}

# ── tool detection ────────────────────────────────────────────────────────────

def _run(cmd, cwd=None, capture=True):
    r = subprocess.run(cmd, cwd=cwd, capture_output=capture, text=True)
    return r

def cloc_available():
    return shutil.which("cloc") is not None

def git_version():
    r = _run(["git", "--version"])
    return r.stdout.strip() if r.returncode == 0 else "unknown"

def pygount_version():
    try:
        import pygount
        return getattr(pygount, "__version__", "installed")
    except ImportError:
        return None

# ── SLOC counting ─────────────────────────────────────────────────────────────

# EVIDENCE-BASED generated-file detection (size is NOT a criterion). A source file is
# excluded from the size metric ONLY if it declares itself generated via an explicit
# in-content marker - the same conventions GitHub's `linguist` uses. These phrases do
# not occur in hand-authored code except to announce generation, so false positives
# are effectively nil. A huge hand-written file with no marker is counted in full; a
# tiny generated file with a marker is excluded. Both raw and adjusted KSLOC are
# reported and every exclusion is logged, so the effect is fully auditable/reversible.
GEN_MARKER = re.compile(
    r"@generated\b"
    r"|do[\s\-]?not[\s\-]?edit"
    r"|do not modify this file"
    r"|automatically[\s\-]?generated"
    r"|auto[\s\-]?generated"
    r"|machine[\s\-]?generated"
    r"|code generated by"
    r"|generated by [\w .\-]+\bdo not\b"
    r"|this (file|code|module) (is|was) (auto[- ]?)?generated"
    r"|statically generated code"
    r"|@nocheck",
    re.I,
)
GEN_SCAN_BYTES = 5_000_000   # scan up to ~5 MB of a file's content for a marker

def _generated_marker(path):
    """Return the matched generation-marker string if the file declares itself
    generated, else ''. Decision is content-only; file size is never consulted."""
    try:
        with open(path, "r", encoding="utf-8", errors="ignore") as fh:
            text = fh.read(GEN_SCAN_BYTES)
        m = GEN_MARKER.search(text)
        return m.group(0) if m else ""
    except Exception:
        return ""

def count_sloc_cloc(repo_dir, subdir=""):
    target = str(Path(repo_dir) / subdir) if subdir else str(repo_dir)
    excl = ",".join(EXCLUDE_DIRS)
    # --by-file lets us test each file for a self-declared generation marker.
    r = _run(["cloc", "--by-file", "--json", f"--exclude-dir={excl}", target])
    if r.returncode != 0:
        raise RuntimeError(f"cloc failed: {r.stderr[:300]}")
    data = json.loads(r.stdout)
    files = {k: v for k, v in data.items() if k not in ("header", "SUM")}
    all_lines = 0; code_lines = 0; code_lines_raw = 0
    lang_tot: dict[str, int] = {}; excluded = []  # (path, code, marker)
    for fpath, fmeta in files.items():
        c = fmeta.get("code", 0)
        lang = fmeta.get("language", "")
        is_src = lang in SOURCE_LANGS
        if is_src:
            code_lines_raw += c                 # raw = everything, nothing excluded
        # resolve the on-disk path cloc reported (it may be relative)
        ondisk = fpath if os.path.isabs(fpath) and os.path.exists(fpath) else str(Path(repo_dir) / fpath)
        marker = _generated_marker(ondisk)
        if marker:
            excluded.append((fpath, c, marker))
            continue                            # evidenced generated -> drop from size
        all_lines += c
        lang_tot[lang] = lang_tot.get(lang, 0) + c   # all languages (top_langs diagnostic)
        if is_src:
            code_lines += c
    ksloc_all = all_lines / 1000
    ksloc_code = code_lines / 1000
    ksloc_code_raw = code_lines_raw / 1000
    gen_loc = sum(c for _, c, _ in excluded if True)  # total LOC removed as generated
    top = sorted(lang_tot.items(), key=lambda x: x[1], reverse=True)[:5]
    return ksloc_code, ksloc_all, dict(top), ksloc_code_raw, gen_loc, excluded

def count_sloc_pygount(repo_dir, subdir=""):
    from pygount import SourceAnalysis
    root = Path(repo_dir) / subdir if subdir else Path(repo_dir)
    code = all_code = code_raw = 0
    langs: dict[str, int] = {}; excluded = []
    for path in root.rglob("*"):
        if path.is_dir():
            continue
        if any(ex in path.parts for ex in EXCLUDE_DIRS):
            continue
        if path.suffix.lower() not in CODE_EXTS:
            continue
        try:
            ana = SourceAnalysis.from_file(str(path), "pygount")
        except Exception:
            continue
        if ana.state_name != "analyzed":
            continue
        lines = ana.code_count
        code_raw += lines
        marker = _generated_marker(str(path))   # same evidence-based rule
        if marker:
            excluded.append((str(path), lines, marker)); continue
        all_code += lines
        code += lines
        lang = ana.language or path.suffix
        langs[lang] = langs.get(lang, 0) + lines
    ksloc_code = code / 1000
    ksloc_all = all_code / 1000
    ksloc_code_raw = code_raw / 1000
    gen_loc = sum(c for _, c, _ in excluded)
    top = sorted(langs.items(), key=lambda x: x[1], reverse=True)[:5]
    return ksloc_code, ksloc_all, dict(top), ksloc_code_raw, gen_loc, excluded

def count_sloc(repo_dir, subdir=""):
    if cloc_available():
        return count_sloc_cloc(repo_dir, subdir)
    return count_sloc_pygount(repo_dir, subdir)

# ── git helpers ───────────────────────────────────────────────────────────────

def _reachable(repo_dir, sha):
    return _run(["git", "cat-file", "-e", sha + "^{commit}"], cwd=repo_dir).returncode == 0

def commit_date(repo_dir, sha):
    """Committer date (YYYY-MM-DD) of a resolved sha, or '' if unknown."""
    r = _run(["git", "show", "-s", "--format=%cs", sha], cwd=repo_dir)
    return r.stdout.strip()[:10] if r.returncode == 0 else ""

def resolve_commit(repo_dir, commit_sha="", cutoff_date=""):
    """Return (sha, source). source in {commit, cutoff, head}. A pinned commit
    that is NOT in this repo (e.g. it belonged to a sibling repo in the delivery)
    is rejected and we fall back to cutoff/HEAD so we never silently mis-measure."""
    if commit_sha:
        sha = commit_sha.strip()
        if not _reachable(repo_dir, sha):
            _run(["git", "fetch", "--quiet", "origin", sha], cwd=repo_dir)  # try to get it
        if _reachable(repo_dir, sha):
            cd = (cutoff_date or "").strip()[:10]
            if cd:
                cdate = _run(["git", "show", "-s", "--format=%cs", sha], cwd=repo_dir).stdout.strip()
                if cdate and cdate > cd:
                    pass  # commit post-dates delivery -> invalid as-delivered pin, fall through
                else:
                    return sha, "commit"
            else:
                return sha, "commit"
        # else: unreachable or post-cutoff -> fall through to cutoff/head
    if cutoff_date:
        r = _run(["git", "log", f"--before={cutoff_date} 23:59:59", "--format=%H", "-n1"], cwd=repo_dir)
        sha = r.stdout.strip()
        if sha:
            return sha, "cutoff"
    r = _run(["git", "log", "--format=%H", "-n1"], cwd=repo_dir)
    return r.stdout.strip(), "head"

# Grounded effort window (Phase 1): the development span is MEASURED from git, not
# assumed. START = the first commit reachable from the delivered commit (project
# inception for grant-dedicated repos); END = the delivered commit's own date. No
# planned values, no synthetic fallback. (Phase 2 will add an idle-trimmed "active
# span" as a second method for a calendar-span vs active-effort study.)
DURATION_PLAUSIBLE_MAX_MONTHS = 24.0   # provisional review flag; > this may carry pre-grant history

def first_commit_date(repo_dir, ref):
    """Date (YYYY-MM-DD) of the EARLIEST commit reachable from ref = inception of the
    as-delivered history. '' if unknown."""
    r = _run(["git", "log", ref, "--reverse", "--no-merges", "--format=%cs"], cwd=repo_dir)
    if r.returncode != 0:
        return ""
    for line in r.stdout.splitlines():
        line = line.strip()
        if line:
            return line[:10]
    return ""

def months_between(d_start, d_end):
    """Calendar months between two YYYY-MM-DD dates (float), or '' if unparseable."""
    try:
        a = datetime.datetime.strptime(d_start[:10], "%Y-%m-%d")
        b = datetime.datetime.strptime(d_end[:10], "%Y-%m-%d")
        return round((b - a).days / 30.44, 2)
    except Exception:
        return ""

# ── PERSON-MONTH definition (Boehm COCOMO II + ISO/IEC/IEEE 15939 documented unit) ──
# COCOMO II nominal: 1 PM = 152 person-hours = 19 working days x 8 h (PH/PM is an
# explicit, adjustable parameter; default 152). Git yields ACTIVITY, not hours, so the
# true PM is irreducibly bounded; we report three transparent estimates and headline
# the Boehm-anchored midpoint:
#   PM_high = active author-MONTHS (>=1 commit in a calendar month => 1.0 PM)   [over-counts]
#   PM_mid  = active developer-DAYS / 19      (Boehm 152 h @ 8 h/active-day)     [headline]
#   PM_low  = time-window HOURS / 152         (git-hours method, reimplemented)  [under-counts]
# Every parameter below is stated and adjustable; the truth lies in [PM_low, PM_high].
PH_PER_PM       = 152.0   # COCOMO II nominal person-hours per person-month (Boehm)
HOURS_PER_DAY   = 8.0     # COCOMO II working day -> DAYS_PER_PM = 152/8 = 19 (exact)
DAYS_PER_PM     = PH_PER_PM / HOURS_PER_DAY            # = 19.0
SESSION_GAP_MIN = 120.0   # git-hours default: commits within this gap = one coding session
FIRST_COMMIT_MIN= 120.0   # git-hours default: buffer added per session for unseen pre-work

def _git_commit_times(repo_dir, ref, since_date="", cutoff_date=""):
    """Return {author_email: [datetime,...]} for non-merge, non-bot commits in window."""
    import datetime as _dt
    cmd = ["git", "log", ref, "--no-merges", "--format=%ae%x09%aI"]   # ISO-8601 strict
    if since_date:
        cmd += [f"--since={since_date}"]
    if cutoff_date:
        cmd += [f"--until={cutoff_date} 23:59:59"]
    r = _run(cmd, cwd=repo_dir)
    by_author: dict[str, list] = {}
    if r.returncode != 0:
        return by_author
    for line in r.stdout.splitlines():
        line = line.strip()
        if not line or BOTS.search(line):
            continue
        parts = line.split("\t")
        if len(parts) != 2:
            continue
        email, iso = parts[0].lower(), parts[1].strip()
        try:
            t = _dt.datetime.fromisoformat(iso)
        except Exception:
            continue
        by_author.setdefault(email, []).append(t)
    return by_author

def _session_hours(times):
    """git-hours time-window estimate for one author's commit datetimes (hours)."""
    if not times:
        return 0.0
    ts = sorted(times)
    minutes = FIRST_COMMIT_MIN          # first session's opening buffer
    for prev, cur in zip(ts, ts[1:]):
        gap = (cur - prev).total_seconds() / 60.0
        if gap <= SESSION_GAP_MIN:
            minutes += gap              # same session: count the worked gap
        else:
            minutes += FIRST_COMMIT_MIN # new session: add its opening buffer
    return minutes / 60.0

def measure_effort(repo_dir, ref, since_date="", cutoff_date=""):
    """Return dict with the three grounded PM estimates + activity counts."""
    by_author = _git_commit_times(repo_dir, ref, since_date, cutoff_date)
    author_months = set(); author_days = set(); total_commits = 0; total_hours = 0.0
    for email, times in by_author.items():
        total_commits += len(times)
        total_hours += _session_hours(times)
        for t in times:
            author_months.add((email, t.strftime("%Y-%m")))
            author_days.add((email, t.strftime("%Y-%m-%d")))
    pm_high = float(len(author_months))               # active author-months
    pm_mid  = round(len(author_days) / DAYS_PER_PM, 4)  # active dev-days / 19
    pm_low  = round(total_hours / PH_PER_PM, 4)        # time-window hours / 152
    return dict(pm_high=pm_high, pm_mid=pm_mid, pm_low=pm_low,
                active_dev_days=len(author_days),
                total_commits=total_commits,
                distinct_authors=len(by_author))

# ── clone / update ────────────────────────────────────────────────────────────

def ensure_clone(project_id, repo_url):
    dest = CACHE / project_id
    if dest.exists():
        r = _run(["git", "fetch", "--quiet"], cwd=dest)
        if r.returncode != 0:
            shutil.rmtree(dest)
        else:
            return dest
    dest.parent.mkdir(parents=True, exist_ok=True)
    # Treeless partial clone: full COMMIT history (needed for effort-window git log
    # and date resolution) but blobs fetched on demand at checkout. Keeps the census
    # (hundreds of repos) within CI disk/time. Falls back to a normal clone if the
    # server refuses partial clone.
    r = _run(["git", "clone", "--quiet", "--filter=blob:none", repo_url, str(dest)])
    if r.returncode != 0:
        r = _run(["git", "clone", "--quiet", repo_url, str(dest)])
    if r.returncode != 0:
        raise RuntimeError(f"git clone failed: {r.stderr[:300]}")
    return dest

# ── main ──────────────────────────────────────────────────────────────────────

FIELDNAMES = [
    "project_id", "project_name", "repo_url", "resolved_commit", "commit_source",
    "effort_since", "effort_until", "actual_duration_months", "duration_plausible",
    "ksloc_code", "ksloc_code_raw", "generated_loc_excluded", "generated_files_n",
    "ksloc_all",
    "pm_mid", "pm_low", "pm_high", "active_dev_days", "total_commits",
    "distinct_authors", "effort_reliable", "top_langs",
    "planned_fte", "planned_duration_months",
    "planned_pm", "cost_usd", "status", "error_msg",
]

# An effort measurement is treated as RELIABLE only when the repo shows genuine
# distributed development in the window: >=2 distinct authors AND >=10 commits.
# This screens out (a) squash/single-import repos (AdMeta, AgriDot: 0 history) and
# (b) single-committer funnelling where one account masks a real team (Societal:
# 71 commits / 1 author -> author-months under-counts). Unreliable rows are KEPT
# (for a sensitivity table) but flagged so the headline fit uses the clean subset.
EFFORT_MIN_AUTHORS = 2
EFFORT_MIN_COMMITS = 10

def is_effort_reliable(active_pm, total_commits, distinct_authors):
    return int(distinct_authors >= EFFORT_MIN_AUTHORS and total_commits >= EFFORT_MIN_COMMITS)

def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--manifest", default=str(HERE / "projects_manifest.csv"))
    ap.add_argument("--out",      default=str(HERE / "measurements.csv"))
    ap.add_argument("--only",     default="", help="comma-separated project_ids to run")
    ap.add_argument("--force",    action="store_true", help="re-measure OK rows")
    ap.add_argument("--max",      type=int, default=0, dest="max_rows",
                    help="measure at most N rows that still need work this run (0=all); resumable across runs")
    ap.add_argument("--only-with-planned", action="store_true", dest="only_planned",
                    help="measure only rows that have a parsed planned_pm (the verified-application subset, e.g. the first ~47)")
    ap.add_argument("--min-commits-per-month", type=int, default=1, dest="min_commits")
    a = ap.parse_args()

    only = {x.strip() for x in a.only.split(",") if x.strip()}
    measured_this_run = 0

    manifest = list(csv.DictReader(open(a.manifest, encoding="utf-8")))

    # load existing results to support resumability
    existing: dict[str, dict] = {}
    if os.path.exists(a.out):
        for row in csv.DictReader(open(a.out, encoding="utf-8")):
            existing[row["project_id"]] = row

    results = []
    for row in manifest:
        pid = row["project_id"]
        if only and pid not in only:
            # preserve existing result if skipping
            if pid in existing:
                results.append(existing[pid])
            continue

        prev = existing.get(pid, {})
        if not a.force and prev.get("status") == "OK":
            print(f"  SKIP (already OK): {pid}")
            results.append(prev)
            continue

        repo_url = row.get("repo_url", "").strip()
        if not repo_url:
            print(f"  NO_REPO: {pid} — fill repo_url in manifest")
            results.append({**{f: "" for f in FIELDNAMES},
                            "project_id": pid, "project_name": row["project_name"],
                            "planned_pm": row.get("planned_pm",""),
                            "planned_fte": row.get("planned_fte",""),
                            "planned_duration_months": row.get("planned_duration_months",""),
                            "cost_usd": row.get("cost_usd",""),
                            "status": "NO_REPO"})
            continue

        # subset filter: phase-1 measures only the verified-application rows (those
        # with a parsed planned_pm). Non-selected rows are preserved if already
        # measured, else simply not emitted this run.
        if a.only_planned and not str(row.get("planned_pm", "")).strip():
            if prev:
                results.append(prev)
            continue

        # batch cap: once this run has measured --max rows, leave the rest for a
        # later run (preserve any prior result; otherwise carry the row as PENDING).
        if a.max_rows and measured_this_run >= a.max_rows:
            if prev:
                results.append(prev)
            else:
                results.append({**{f: "" for f in FIELDNAMES},
                                "project_id": pid, "project_name": row.get("project_name", pid),
                                "repo_url": repo_url,
                                "planned_pm": row.get("planned_pm", ""),
                                "planned_fte": row.get("planned_fte", ""),
                                "planned_duration_months": row.get("planned_duration_months", ""),
                                "cost_usd": row.get("cost_usd", ""), "status": "PENDING"})
            continue

        print(f"  Measuring: {pid} ({repo_url})")
        measured_this_run += 1
        out_row = {f: "" for f in FIELDNAMES}
        out_row.update({"project_id": pid, "project_name": row["project_name"],
                        "repo_url": repo_url,
                        "planned_pm": row.get("planned_pm",""),
                        "planned_fte": row.get("planned_fte",""),
                        "planned_duration_months": row.get("planned_duration_months",""),
                        "cost_usd": row.get("cost_usd","")})
        try:
            repo_dir = ensure_clone(pid, repo_url)
            sha, csource = resolve_commit(repo_dir, row.get("commit_sha",""), row.get("cutoff_date",""))
            co = _run(["git", "checkout", "--quiet", "-f", sha], cwd=repo_dir)
            if co.returncode != 0:
                raise RuntimeError(f"checkout failed for {sha[:12]}: {co.stderr[:160]}")
            kc, ka, top, kc_raw, gen_loc, gen_files = count_sloc(repo_dir, row.get("subdir",""))
            for gp, gc, gm in gen_files:        # audit log: every excluded file + its evidence
                print(f"      [generated excluded] {gc:>6} LOC  marker='{gm}'  {gp}")
            # GROUNDED effort window (Phase 1), fully measured from git:
            #   END   = the delivered commit's own committer date (as-delivered);
            #   START = the first commit reachable from it (project inception), unless
            #           the manifest gives an explicit since_date override.
            # actual_duration_months is the MEASURED calendar span (no planned values).
            cutoff = commit_date(repo_dir, sha) or row.get("cutoff_date", "")
            since = (row.get("since_date", "") or "").strip() or first_commit_date(repo_dir, sha)
            dur = months_between(since, cutoff) if (since and cutoff) else ""
            dur_ok = int(isinstance(dur, (int, float)) and 0 < dur <= DURATION_PLAUSIBLE_MAX_MONTHS)
            eff = measure_effort(repo_dir, sha, since, cutoff)
            tc = eff["total_commits"]; da = eff["distinct_authors"]
            rel = is_effort_reliable(eff["pm_high"], tc, da)
            out_row.update({"resolved_commit": sha, "commit_source": csource,
                            "effort_since": since, "effort_until": cutoff,
                            "actual_duration_months": (f"{dur}" if dur != "" else ""),
                            "duration_plausible": str(dur_ok),
                            "ksloc_code": f"{kc:.4f}", "ksloc_code_raw": f"{kc_raw:.4f}",
                            "generated_loc_excluded": str(gen_loc),
                            "generated_files_n": str(len(gen_files)),
                            "ksloc_all": f"{ka:.4f}",
                            "pm_mid": f'{eff["pm_mid"]}', "pm_low": f'{eff["pm_low"]}',
                            "pm_high": f'{eff["pm_high"]}', "active_dev_days": str(eff["active_dev_days"]),
                            "total_commits": str(tc), "distinct_authors": str(da),
                            "effort_reliable": str(rel),
                            "top_langs": json.dumps(top), "status": "OK"})
            print(f"    OK [{csource}] {since or '-'}..{cutoff or '-'} ({dur}mo): ksloc_code={kc:.2f}  "
                  f"PM_low={eff['pm_low']} PM_mid={eff['pm_mid']} PM_high={eff['pm_high']}  authors={da}")
        except Exception as e:
            out_row["status"] = "ERROR"
            out_row["error_msg"] = str(e)[:200]
            print(f"    ERROR: {e}")
        results.append(out_row)

    with open(a.out, "w", newline="", encoding="utf-8") as f:
        w = csv.DictWriter(f, fieldnames=FIELDNAMES)
        w.writeheader()
        w.writerows(results)

    prov = {
        "run_date": datetime.datetime.utcnow().isoformat() + "Z",
        "git_version": git_version(),
        "sloc_tool": "cloc" if cloc_available() else f"pygount {pygount_version()}",
        "min_commits_per_month": a.min_commits,
        "exclude_dirs": EXCLUDE_DIRS,
        "manifest": a.manifest,
    }
    json.dump(prov, open(HERE / "measurement_provenance.json", "w"), indent=2)

    ok = sum(1 for r in results if r["status"] == "OK")
    no_repo = sum(1 for r in results if r["status"] == "NO_REPO")
    err = sum(1 for r in results if r["status"] == "ERROR")
    print(f"\nDone. {ok} OK | {no_repo} NO_REPO | {err} ERROR")
    print(f"Wrote: {a.out}")
    if no_repo:
        print(f"  -> Fill repo_url for {no_repo} projects in projects_manifest.csv, then re-run.")

if __name__ == "__main__":
    main()
